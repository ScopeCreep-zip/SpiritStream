{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
    "profile": {
      "description": "Full profile body (matches the `Profile` ts-rs export). Encoding-config\nrules (bitrate / keyframe / resolution / fps) are evaluated server-side."
    }
  },
  "required": [
    "profile"
  ],
  "title": "StreamValidateRequest",
  "type": "object"
}
