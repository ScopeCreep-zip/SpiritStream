{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
    "errors": {
      "description": "Per-subsystem failure detail. Empty when `ready` is true.",
      "items": {
        "$ref": "#/components/schemas/ReadyCheckFailure"
      },
      "type": "array"
    },
    "failed": {
      "description": "Names of subsystems that failed their check. Empty when `ready` is true.",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "ready": {
      "description": "`true` when every critical subsystem reports healthy.",
      "type": "boolean"
    }
  },
  "required": [
    "ready",
    "failed",
    "errors"
  ],
  "title": "ReadyResponse",
  "type": "object"
}
