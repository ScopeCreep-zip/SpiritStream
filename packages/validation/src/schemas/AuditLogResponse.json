{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
    "chain": {
      "description": "HMAC chain status. The audit-log UI inspects this on\nevery fetch; a `tampered` value triggers the red banner.\nAlways computed server-side against the on-disk log; clients cannot influence it."
    },
    "entries": {
      "description": "Page slice (oldest-first within the returned window).",
      "items": {},
      "type": "array"
    },
    "total": {
      "description": "Total entries that survive the filter (before paging).",
      "minimum": 0,
      "type": "integer"
    }
  },
  "required": [
    "total",
    "entries",
    "chain"
  ],
  "title": "AuditLogResponse",
  "type": "object"
}
