{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Per-subsystem status report.\n\nEach variant maps cleanly to a UI badge color: `ok` \u2192 green,\n`degraded` \u2192 amber, `disconnected`/`tampered` \u2192 red. Adding a new\nvariant is a deliberate act \u2014 every subsystem reports through this\nenum so the surface stays bounded.",
  "discriminator": {
    "propertyName": "state"
  },
  "oneOf": [
    {
      "properties": {
        "state": {
          "enum": [
            "ok"
          ],
          "type": "string"
        }
      },
      "required": [
        "state"
      ],
      "type": "object"
    },
    {
      "description": "Functioning but in a degraded state. `detail` carries the\nreason (e.g. `\"FFmpeg not on PATH; using bundled binary\"`).",
      "properties": {
        "detail": {
          "type": "string"
        },
        "state": {
          "enum": [
            "degraded"
          ],
          "type": "string"
        }
      },
      "required": [
        "detail",
        "state"
      ],
      "type": "object"
    },
    {
      "description": "Subsystem requires user attention. `detail` describes why.",
      "properties": {
        "detail": {
          "type": "string"
        },
        "state": {
          "enum": [
            "disconnected"
          ],
          "type": "string"
        }
      },
      "required": [
        "detail",
        "state"
      ],
      "type": "object"
    },
    {
      "description": "Audit-log tamper detection found a chain break. The number is\nthe last verified sequence so the UI can render the red banner.",
      "properties": {
        "last_valid_sequence": {
          "format": "int64",
          "minimum": 0,
          "type": "integer"
        },
        "state": {
          "enum": [
            "tampered"
          ],
          "type": "string"
        }
      },
      "required": [
        "last_valid_sequence",
        "state"
      ],
      "type": "object"
    }
  ],
  "title": "SubsystemStatus"
}
