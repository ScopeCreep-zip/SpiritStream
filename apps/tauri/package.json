{
  "name": "@spiritstream/tauri",
  "version": "1.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build:server": "tsx ../../scripts/build-server.ts",
    "build:server:release": "tsx ../../scripts/build-server.ts --release",
    "fetch:ffmpeg": "tsx ../../scripts/fetch-bundled-ffmpeg.ts",
    "dev": "pnpm run fetch:ffmpeg && pnpm run build:server && tsx ../../scripts/dev-desktop.ts",
    "build": "pnpm run fetch:ffmpeg && pnpm run build:server:release && tauri build",
    "build:debug": "pnpm run fetch:ffmpeg && pnpm run build:server && tauri build --debug",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.6",
    "tsx": "^4.21.0"
  },
  "dependencies": {
    "@tauri-apps/plugin-http": "~2.5.6"
  }
}
